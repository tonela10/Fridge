package com.sedilant.cachosfridge.ui.theme

import androidx.compose.ui.graphics.Color

// ── Brand colours ─────────────────────────────────────────────────────────────
val GreenPrimary     = Color(0xFF5DD62C)   // bright green  – primary action
val GreenDark        = Color(0xFF337418)   // dark green    – primary container / variant
val GreenOnDark      = Color(0xFFD6F5C6)   // light mint    – text on green surfaces

// ── Neutral backgrounds ────────────────────────────────────────────────────────
val BackgroundBlack  = Color(0xFF0F0F0F)   // deepest black – background
val SurfaceDark      = Color(0xFF202020)   // dark gray     – card / surface
val SurfaceVariant   = Color(0xFF2A2A2A)   // slightly lighter card
val SurfaceContainer = Color(0xFF1A1A1A)   // between background and surface

// ── Text / icon colours ────────────────────────────────────────────────────────
val OnBackground     = Color(0xFFF8F8F8)   // near-white    – primary text
val OnSurface        = Color(0xFFF8F8F8)
val OutlineColor     = Color(0xFF5A5A5A)   // muted gray    – dividers / hints

// ── Semantic helpers ───────────────────────────────────────────────────────────
val ErrorRed         = Color(0xFFFF5252)
val OnErrorRed       = Color(0xFFFFFFFF)
